#include <bits/stdc++.h>
using namespace std;

int main()
{
    
  ios_base::sync_with_stdio(0); cin.tie(NULL);  cout.tie(NULL);
  int n, m; cin >> n; m = sqrt(n);  cout << "The largest square has side length " << m << ".\n";
  
  return 0;
}
